# tarot-reader
POC for a tarot reader app.

Card deck is held as tuples (pid, name, upright meaning, reversed meaning).
Shuffled draw returns (pid, orientation)
Dispatch routes shuffle instructions by spread
Prints drawn tuple + meaning to console

## run:
python _draw_cards.py_
